define([
    "jquery",
    "underscore",
    "backbone",
    "conf",
    "models/Company"], function($, _, Backbone, Conf, Company) {
    "use strict";

    // Définir une collection de Company

    return {};
});